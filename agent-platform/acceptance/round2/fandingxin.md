# 第二轮回归验收报告 · 范丁鑫（developer / 流程革新部 IT 专员，技术视角）

- person_id=12，token 来自 `POST /api/login {"person_id":12}`
- 复测方式：python urllib 并发压测脚本 + API 边界用例 + Chrome headless 截图（900×900 窄屏 ×2、1440×900 ×1）
- 测试时间：2026-07-20 上午；数据库为干净种子态（本轮状态枚举已改为 规划中/开发中/试运行/已上线/已下线）

## 一、第一轮问题复测结果（逐项）

### 1. [阻塞] 并发 500 —— ✅ 已修复

- 第一轮：≥5 路并发读、≥2 路并发写即全部 500（10 线程 20 次并发写 20/20 全崩）。
- 本轮：`acceptance/round2/_concurrency_test.py`，20 线程混合读写（GET /api/agents 与 POST /api/workspaces/1/messages 交替），每线程 10 次，共 3 轮：
  - 第1轮：200/200 全部 200，耗时 2.26s，500 数 0
  - 第2轮：200/200 全部 200，耗时 2.37s，500 数 0
  - 第3轮：200/200 全部 200，耗时 2.19s，500 数 0
- 合计 600 次混合并发请求，500 率 0.0%（合格线 <2%）。阻塞解除。

### 2. [严重] PATCH status 无枚举校验 —— ✅ 已修复

- 第一轮：`"飞天状态"` 返回 200 直接入库。
- 本轮（用 coach 李乐平 token 测，developer 已无 PATCH 权限见第 3 条）：
  - `PATCH /api/agents/6 {"status":"飞"}` → **422** `{"detail":"status 仅允许：规划中/开发中/试运行/已上线/已下线，收到「飞」"}`
  - 旁证：`停用`、`试点中` 等非法值同样 422，且报文把允许值和收到的值都写出来，中文、可直接读给业务方听
  - 复验 GET /api/agents/6 状态未被污染（仍为"开发中"），脏数据无法入库

### 3. [一般] 越权：developer 可改任意部门员工 —— ✅ 已修复（且超出预期）

- 本轮权限矩阵实测：

| 身份 | 操作 | 结果 |
|---|---|---|
| staff 张悦 PATCH 本部门员工 | 改状态 | **403** `仅高管/教练团或该数字员工的负责人本人可修改` |
| developer 范丁鑫 PATCH 本部门他人负责的员工 | 改状态 | **403** 同上中文提示 |
| developer 范丁鑫 PATCH 自己负责的员工（id=7 运维数字员工） | 改状态 | **200**，改回亦 200，审计有留痕 |
| coach 李乐平 PATCH 外部门员工 | 改状态 | 200（教练团按设计有权） |
| 无 token PATCH | — | 401 `未登录或缺少 Token` |

- 从"无任何资源级授权"到"高管/教练团/负责人本人"三级模型，第一轮第 5 条关闭。
- 测试说明：验证 coach 外部门权限时曾把 agent1 误改为"已上线"，已按 seed.py（董事办 wave1、无 override → 开发中）立即改回并复验，审计接口可见对应"更新数字员工"留痕。

### 4. [一般] 移动端窄屏 —— ✅ 已修复

- 第一轮：800px 下侧栏固定占约 29%，不折叠。
- 本轮 900×900 截图（fandingxin_narrow_agents.png / fandingxin_narrow_knowledge.png）：
  - 左侧导航已完全折叠，顶栏出现**汉堡按钮 ☰**，标题居中，内容区占满全宽
  - 数字员工卡片 2 列排布不挤压；知识库空间卡片 2 列、文档五列表格在 900px 下完整可见无横向溢出
  - 1440px 宽屏（fandingxin_wide_knowledge.png）侧栏正常展开，无回归

### 5. [建议] 知识库 L4 无样例 —— ✅ 已修复

- `GET /api/knowledge/documents`：22 篇文档，密级分布 L1×3 / L2×10 / L3×7 / **L4×2**，四级齐全
- L4 样例：`L4 核心配方·示例（仅示意）`（研发平台空间，标签 配方/核心机密）、`L4 核心工艺参数·示例（仅示意）`（智造质量平台空间，标签 工艺/核心机密）
- 前端 `LEVEL_META` 已含 `L4: 'bg-danger'`（红色徽章），选中对应空间即可看到
- 空间命名规范：6 个空间统一为"XX平台空间"格式（战略平台财务空间/战略平台空间/产品营销平台空间/研发平台空间/智造质量平台空间）+ 榕器创共享空间，设备容量字段完整

### 6. [建议] 空状态 —— ✅ 已修复

- app.js 现有 10 处 `emptyHtml` 引导文案，覆盖知识库（"这个资料柜还是空的，点右上角「登记文档」把公司文件放进来"）、工作区、消息区、数字员工筛选、场景、激励、报销、审计等列表，第一轮"裸表头"问题关闭。

### 7. [一般] 治理中心默认页签 —— ⚠️ 部分改善

- 默认仍停在"激励申报"（govState.tab='incentives' 未变）；但页签已支持 URL 直达（`#/governance/audits` 等子路径可定位），可分享给他人直链。入口显眼度问题仍在，降级为建议。

### 8. [一般] 422 英文报文 —— ❌ 未修复

- `POST /api/login {"person_id":"abc"}` → 422 pydantic 原文 `value is not a valid integer`；缺字段 `field required`；`GET /api/agents?wave=abc` 同样英文。业务接口的中文错误已做好（见第 2/3 条），框架层校验错误仍外抛英文，维持第一轮结论（一般）。

### 9. [建议] 审计接口命名 —— ❌ 未变动

- 仍为 `/api/governance/audits`，`audit-logs` 404。建议级，不影响使用。

## 二、新发现问题

1. [建议] 治理中心-审计日志：接口只返回最近 100 条，且本轮验收多人并发登录产生大量"登录"流水，把"更新数字员工/审批"等业务审计快速淹没；建议支持按类型筛选或分页。（页面：治理中心-审计日志；期望：业务操作可追溯；实际：登录流水刷屏后业务条目很快被挤出前 100 条）
2. [建议] 知识库空间命名："战略平台财务空间"所属部门是财务部，与"战略平台空间"（所属战略平台）并排列，第一眼看容易误以为前者也属于战略平台；建议统一为"财务部空间"或在卡片上把"所属"放更醒目位置。（页面：知识库；期望：空间名与所属部门一致；实际：命名按领域、归属按部门，存在轻微歧义）
3. 无阻塞、无严重新问题；并发修复未引入回归（顺序读写、权限、审计均正常）。

## 三、学习门槛重新评分

- 第一轮：4/5 → 本轮：**4.5/5**
- 理由：错误提示全部口语化中文（连非法值都把"收到「飞」"回显），权限规则一句话能看懂（"仅高管/教练团或该数字员工的负责人本人可修改"），空状态有引导，窄屏有汉堡折叠；仍扣 0.5：框架层 422 英文原文、治理中心红线/审计入口层级深、审计接口命名不直觉。

## 四、总体判定

**能落地**。第一轮 1 阻塞（并发 500）+1 严重（status 无校验）均已关闭且验证充分；权限模型、窄屏、L4 样例、空状态等一般/建议项多数闭环。遗留项全部为建议/一般级体验优化（422 英文、审计筛选、命名歧义、治理页签层级），不阻塞 65 人上线。
