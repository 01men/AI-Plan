# 第 2 轮回归验收报告 —— 徐露璐（staff · 外贸跟单员 · 零技术背景）

- person_id=40，tier=staff，国际销售部「业务使用人」，最低学习门槛样本。
- 测试时间：2026-07-20；写操作 500 次数：**0 次**（全部写操作返回 200/403）。
- 截图：`xululu_loginpage.png` / `xululu_loginpage_full.png` / `xululu_login_landing.png` / `xululu_home.png` / `xululu_tasks.png`。

## 一、第一轮问题复测结果

### ① 登录后默认落点（第一轮：落驾驶舱看不懂）—— ✅ 已修复

- 不带 hash 访问 `/?person=40`（模拟正常登录进入），**默认落在「协作空间」**，截图 `xululu_login_landing.png`：左侧导航高亮「协作空间」，顶部说明条「在这里跟数字员工说话、派活、收结果」。
- 她第一眼看到的是工作区列表（第二个就是「试点项目·外贸订单跟单自动化」）和一句大白话说明，不再是 7 个经营指标。
- 注：手动输 `#/` 仍会强制进驾驶舱，但正常登录路径不会走到那里，不算问题。

### ② 视图顶部说明条 + 登录页引导 —— ✅ 已修复（超出预期）

- 每个视图顶部都有说明条，且全是大白话（摘自 app.js VIEW_HINTS 并与截图核对）：协作空间「在这里跟数字员工说话、派活、收结果」、任务中心「数字员工干的活，在这里检查和确认」、数字员工「你的 AI 同事花名册」、场景库「想让 AI 干什么活，从这里提」。她能看懂。
- 登录页（`xululu_loginpage_full.png`）：顶部「你是第一次来？点你的名字就能进去，不用密码不用学。」按身份分组，每组配一行「进去后干什么」：**使用人组写的是「进去后点『协作空间』，找你的数字员工聊天派活」**——这句话就是第一轮她最缺的那句指引。董事长/教练团/骨干/开发者组也各有对应引导。
- 协作空间内部也有细化提示：讨论区「和同事讨论，AI 不打扰」、输入框「和同事聊聊想法……（AI 不会在这里插话）」。

### ③ 用人话 @数字员工派活 + 看懂交付物 —— ✅ 已修复（第一轮即为活路，本轮依然顺畅）

- 在工作区 2「Agent 执行区」发送：`@外贸跟单数字员工 帮我把这周德国客户的订单理一理，哪些快到交期还没发货？列个清单，再把唛头草稿做出来。`
- API 立即返回 `"dispatched": [{"task_id": 8, "agent_id": 22, "agent_name": "外贸跟单数字员工"}]`，秒级派发。
- 任务 #8 状态「待审核」，交付物说人话：「已从邮件/钉钉提取附件 6 份，按客户/订单号/日期归档」「ERP 下单草稿 1 份（待人工确认后提交）」「唛头/不干胶标签 1 套（PDF）」，末尾 3 条「待人工确认事项」。她能看懂这就是她的日常活。
- 遗留小瑕疵（第一轮建议 8 未采纳）：交付物仍出现「NAS」「写回动作留痕」等词，她不知道 NAS 是什么。【建议级】

### ④ 越权审核（第一轮：staff 能审核通过任何部门任务）—— ✅ 已修复

- 她以 staff 身份 `POST /api/tasks/5/review`（研发部何建强的任务）→ **403 `{"detail":"当前身份无权审核任务，仅高管/教练团/骨干可审核"}`**，中文、说人话、告诉她该找谁。
- 连带修好第一轮问题 3：她审核**自己**派的任务 #8 → 同样 403 同一提示。「创建人自审」这条治理漏洞已堵死，任务卡片红字「需业务骨干/教练团审核」与实际逻辑一致了（截图 `xululu_tasks.png` 待审核列）。
- 残留数据瑕疵（用户不可见）：任务 #8 的 reviewer_id 仍被写成 40（她自己），但她已无权限操作，仅后端字段不一致。【一般级，不影响使用】
- 任务中心新增「只看待我审核」复选框（对骨干/教练有用，对她无感）。

### ⑤ 任务中心直建任务（第一轮：静默死路，永不执行）—— ✅ 已修复

- `POST /api/tasks` 直建任务 #11，仍落「待处理」列（不自动执行），但响应新增 `hint` 字段：**「未指派数字员工，任务不会自动执行，建议到协作空间 @数字员工 派活」**；前端 app.js:1038 会在创建时以 toast 弹出该提示。
- 任务中心空列也加了引导文案：「还没有任务，去协作空间 @数字员工派一个吧」（`xululu_tasks.png` 进行中/已驳回列可见）。
- 残留小瑕疵：toast 只在创建瞬间出现，之后 #11 卡片本身不带任何提示，过两天她再看这张卡仍可能疑惑"怎么没动静"。建议卡片上常驻一行小字。【一般级】

## 二、新发现问题

1. 【一般】任务中心直建的任务卡片无常驻提示（仅创建时 toast 一闪而过），页面：任务中心；期望：卡片上标注「未派给数字员工，不会自动执行」；实际：卡片与正常任务外观无异。
2. 【一般】任务 reviewer 字段逻辑：员工自建任务 reviewer_id 仍自动=自己（如 #8 reviewer=徐露璐），与「仅高管/教练团/骨干可审核」的新规则在数据层不一致；页面不可见，但治理报表若取该字段会失真。
3. 【建议】交付物文案仍有「NAS」「写回留痕」术语（任务 #8 交付物第一节/第三节），建议改「公司共享盘」「确认后自动提交并保留记录」。

无阻塞、无严重新问题；未发现修复引入的回归（协作空间、任务中心、登录页均正常渲染）。

## 三、学习门槛重新评分

- 第一轮 **2 / 5** → 本轮 **4 / 5**。
- 理由：登录页「点名字就能进去，不用密码不用学」+ 使用人组那句「进去后点协作空间，找你的数字员工聊天派活」，配合默认落点改为协作空间，她第一次进系统大概率能自己摸到正确入口；每个页面顶部一句大白话说明，术语墙基本拆除；派活→交付物→交付物看得懂这条主线零障碍；越权操作被中文提示温和拦住。扣 1 分：任务中心直建任务仍不执行（虽有提示），「工作区/Agent 执行区」这两个词依然存在，首次@时可能还要人指一下。

## 四、总体判定

**能落地**。第一轮 1 阻塞 + 2 严重全部修复并有中文引导，学习门槛 2→4 达到合格线（≥4），写操作零 500。她这个零技术跟单员现在可以不靠人教完成「登录→协作空间→@数字员工→看懂交付物→等骨干审核」全流程。
