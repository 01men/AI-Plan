# 第二轮回归验收报告 · 李丹（backbone · 人力资源部）

- 环境：http://127.0.0.1:8000，未重启/未改代码；person_id=8，token 登录；方式=无头 Chrome 截图 + API（python urllib，中文 utf-8）。
- 数据库为干净种子态。本轮写操作 4 次（激励申报 POST×4），**HTTP 500 发生 0 次**（并发锁定问题未复发）。
- 截图：`lidan_org.png`（1440×2400 全页）、`lidan_skills.png`、`lidan_governance.png`。

## 一、第一轮问题复测结果（指定 4 项）

### ① 组织通讯录：首平台默认展开、使用人徽标可见 — ⚠️ 部分修复

- **首平台默认展开 ✅**：打开 `#/org`，"战略平台"（我所在平台）默认展开，8 个部门、人员+数字员工+梯队徽标（董事长金/教练团绿/业务骨干深蓝/开发者橙）直接可见，无需任何点击。证据：`lidan_org.png` 上半屏。
- **使用人徽标首屏仍不可见 ❌**：API 核对（`GET /api/org/tree`）确认 9 名使用人（staff）全部分布在**产品营销平台（徐露璐、张悦）、智造平台（李建东、王鑫、梅鹏宇）、研发平台（张卫、林社然、李鑫、徐文熠）**——这 4 个平台**仍然默认折叠**，折叠行只有"编制 34 人 · 6 部门 · 8 人员 · 11 数字员工"这类汇总数字，**没有任何梯队徽标**。证据：`lidan_org.png` 底部折叠行特写（y≈1340-1600）。
- 补充：前端代码（/static/app.js）已定义 `staff: { label: '使用人', badge: 'bg-gray-400' }`，即徽标渲染能力存在，只是被折叠挡住了。页面顶部横幅也无梯队图例。
- 结论：第一轮"首屏看不到使用人"的卡点**只修了一半**——展开默认值对了，但使用人恰好都不在首平台。建议二期要么默认全展开，要么在折叠行追加梯队徽标摘要（如"使用人×2"）。

### ② 激励申报档位校验（火花奖报 3000 应被拒） — ✅ 已修复

API 实测（`POST /api/governance/incentives`，均带 token）：

| 用例 | 结果 |
|---|---|
| 火花奖 / 3000 元 | **HTTP 422**："火花奖申报金额须在 500-2000 元之间，当前 3000 元超出档位" ✅ |
| 火花奖 / 100 元（低于下限） | **HTTP 422**，同上提示 ✅ |
| 火花奖 / 800 元（合法） | **HTTP 200**，`{"id":4,"status":"申报中",...}`，列表页即时可见（`lidan_governance.png` 首行"火花奖/徐婧/¥800/申报中"）✅ |
| 金扳手奖 / 99999 元 | **HTTP 422**："金扳手奖申报金额须在 30000-50000 元之间…" ✅ |

上下限都拦、报错文案直接告诉我合法区间，不用猜。附带发现：治理中心报销 Tab 已从"Token 报销"改名为 **"AI 算力费用报销（Token）"**——第一轮第 7 条术语问题也顺带改善 ✅。

### ③ "人"级考核数据缺口（第一轮严重） — ❌ 未修复，但评估为**可接受为二期需求**

- 现状核查：openapi.json 全部 24 个端点中仍只有 `/api/metrics/agents`、`/api/metrics/dashboard`；`/api/metrics/people`、`/api/metrics/persons` 均 **404**；`GET /api/people/8` 返回只有档案字段（姓名/部门/tier/agents），**无任何个人行为/产出数据**。dashboard 产出榜仍纯数字员工维度。与第一轮完全相同。
- **HR 视角再评估（本轮核心判断）**：这仍是事实性缺口，但我重新权衡后认为**不必阻塞落地**，理由三条：
  1. 平台定位是"AI 进度管理与人机协同"，一期考核对象是**数字员工与项目**（tasks_done/hours_saved/accuracy 已齐），这部分数据闭环已成立；
  2. 第一轮另外两个严重项已修复（见下"顺带复核"），作为考核**数据源**的口径可信性已恢复——数据可信之后，"考机器人"已经能用，"考人"是增量需求而非底座缺陷；
  3. 人级数据需要全站埋点（登录/提交/审核行为），工作量级是"二期需求"而非"修复"，强塞一期反而拖垮落地节奏。
- 前提条件（必须写进二期需求清单）：①person 级活跃度与贡献接口；②活跃使用率分母口径标注（335 编制 vs 48 账号）；③部门维度对比；④激励记录汇总到个人档案。**在这些落地前，HR 条线不会把本平台作为员工考核依据，只作为数字员工管理工具使用。**
- 级别调整：严重 → **一般（二期需求）**。

### ④ Skill 库个人空组引导文案 — ✅ 已修复

- `#/skills` 页面"个人 · 共 0 个 Skill"分组不再是裸标题：出现带空状态图标的大卡片，文案 **"还没有个人技能，把你常用的 AI 话术沉淀到这里"**。证据：`lidan_skills.png` 底部。
- 新人现在知道这组是干什么的、为什么是空的。小遗憾：文案说了"沉淀到这里"但**没有给入口**（无按钮、无链接，全站也没找到创建个人 Skill 的表单），引导只完成了一半——见新问题 N1。

## 二、顺带复核（第一轮另两个严重项，虽非指定项但属我报告所提）

- **投入构成口径 ✅ 已修复**：`GET /api/metrics/dashboard` 返回 `breakdown = 算力资源 137,895.6（阿里云 23,850 + 智谱 84,045.6 + 灵活采购 30,000）+ NAS知识库底座 105,000（采购 95,000 + 运维 10,000）+ 创新激励奖金池 100,000`，与方案 13.79/10.5/10 万**逐科目吻合**，总额 342,895.6 ✅。
- **页面与 API 口径矛盾 ✅ 已修复**：API 现返回 coverage=0.0 且新增 `trial_coverage=6.2`（"试点中+已验收"口径拆分）、acceptance_rate=0、active_rate=7.7，与第一轮页面显示值完全一致，且每个 KPI 自带口径 note（如"近7日有产出数字员工/已上线+试运行"）。0% 与 100% 自相矛盾的情况消失。

## 三、新发现问题

| # | 级别 | 页面 | 期望 vs 实际 |
|---|------|------|--------------|
| N1 | 建议 | Skill 库 | 空组引导文案应配套行动入口（按钮/链接，如"去协作空间沉淀第一条话术"）。**实际**只有一句话，看完知道该做但不知道去哪做；且全站未见个人 Skill 创建入口。 |
| N2 | 建议 | 治理中心 | 申报表单应**预提示**所选奖项的档位区间（如选火花奖即显示"500-2000 元"）。**实际**前端 app.js 中无档位文案，用户只能提交后被 422 打回才知道区间——后端校验已合格，前端体验差半步。 |
| N3 | 建议 | 驾驶舱/治理中心 | 创新激励奖金池 10 万已在投入构成中可见，但**已发/剩余额度**仍不可见（现有 3 笔已评定合计 38,800 + 1 笔申报中 800，无法与池子挂钩）。 |

未发现阻塞/严重级新问题；界面无回归（治理中心列表、Skill 分组、通讯录渲染均正常）。

## 四、学习门槛重新评分

- **第一轮 4/5 → 本轮 4/5（维持）**。
- 加分变化：首平台默认展开、空组引导文案、档位报错文案直白、报销 Tab 改名——四处都是减门槛的实修。
- 未加分原因：我第一轮的主要卡点"使用人在哪"**依然存在**（要手动展开 3 个折叠平台）；Teams.md 术语仍在横幅。门槛确实低了，但扣分主因未消除，维持 4 分是诚实评价。
- 是否愿意每天用：**愿意**（作为通讯录+激励申报+技能市场+数字员工产出看板）；HR 员工考核用途待二期数据补齐后启用。

## 五、总体判定

**能落地。** 4 个指定复测项 2 项全修、1 项半修（①，无阻塞性）、1 项未修但经评估合理降级为二期需求（③，附明确前提）；第一轮 3 个严重项（投入口径/页面 API 矛盾/人级数据）前两个已彻底修复，第三个有清晰二期路径；写操作 500 零发生。满足合格线（无阻塞、无在册严重、backbone 线 ≥3 分）。剩余事项：①折叠平台使用人徽标可见性、N1-N3 三个建议项纳入二期清单即可。

---
附：本轮产生业务数据 1 条（激励申报 id=4，火花奖/徐婧/800 元/申报中，合法档位验证用）；被拒绝的 3 次（3000/100/99999）未落库。
