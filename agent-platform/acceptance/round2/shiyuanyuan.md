# 第二轮回归验收报告 · 师圆圆（coach / 数字化项目负责人）

- 日期：2026-07-20 ｜ 环境：http://127.0.0.1:8000 ｜ person_id=3，tier=coach
- 第一轮报告：`acceptance/round1/shiyuanyuan.md`
- 写操作 500 次数：**0**（立项、派活、驳回、通过、私聊共 6 次写操作，另有 1 次预期内 403 业务拦截，非服务异常）

## 一、第一轮问题复测结果（逐项）

### 复测① 敏捷立项后工作区自动带数字员工（第一轮问题1，阻塞）— ✅ 已修复
- 76 个待立项场景 `agent_id` 已全部绑定（第一轮全为 null），场景库"数字员工"列不再显示"未绑定"（证据：`shiyuanyuan_scenarios.png`，每行均有员工名）。
- `POST /api/scenarios/6/initiate` → 200，自动建工作区 #7；`GET /api/workspaces/7` 成员为 2 人：师圆圆（human）+ **财务经营数字员工（agent, member_id=1）**。第一轮此处为 0 个员工。
- 加分项：讨论区"立项说明"系统消息现在写明"**责任数字员工：财务经营数字员工**"，第一轮"不知道该@谁"的问题随之消解（`shiyuanyuan_ws7.png`）。

### 复测② 派活→驳回→重做交付物带修订说明与 v2 角标→再通过（第一轮问题2，严重）— ✅ 已修复
- 讨论区 @财务经营数字员工 派活 → 任务 #7 秒级产出 v1 交付物（payload `version:1`）。
- 驳回（驳回意见："内容太笼统：营收、回款、订单三块均无具体数字，请补充昨日实际金额与环比后再提交。"）→ 200，数字员工立即重做。
- 重做交付物**开头即为**：`第 2 版修订说明：针对上一轮驳回意见『内容太笼统：营收、回款、订单三块均无具体数字，请补充昨日实际金额与环比后再提交。』，本版已逐项修订，请复核。`——修订说明与驳回意见原文均在（第一轮重做件与首版逐字相同）。消息 payload `version:2, rework:true`。
- 界面证据（`shiyuanyuan_ws7_agent.png`）：交付卡片带 **v2 角标** + 橙色"**按驳回意见修订**"徽标 + "待审核"状态标。
- 再通过 → 200，状态"已通过"，`done_at` 落库，讨论区自动发通过通知（含审核人与批注）。

### 复测③ 私聊区发需求，AI 助手回打磨稿 — ✅ 已修复
- 私聊打磨区发送"我想让数字员工每周五自动汇总各部门的经营指标并生成周报初稿"→ 项目管理智能体秒回《需求打磨草稿》：结构化任务草稿（目标/建议交付物/审核节点）+ 建议派活对象 + 可复制的示例话术（`shiyuanyuan_ws7_private.png`）。
- 打磨稿质量小瑕疵见"新发现问题"第 2、3 条。

### 复测④ 投入构成口径（第一轮问题3，严重）— ✅ 已修复
- `GET /api/metrics/dashboard`：`investment.year1=342895.6（34.3万）`，breakdown 三科目：**算力资源 137,895.6**（明细：阿里云节省计划 23,850 + 智谱团队套餐 84,045.6 + 个性化灵活采购 30,000）、**NAS知识库底座 105,000**（采购 95,000 + 运维预留 10,000）、**创新激励奖金池 100,000**——与方案口径（13.79+10.5+10）逐项一致。
- 界面证据（`shiyuanyuan_dashboard.png`）：投入构成三科目与二级明细全部展示，并新增"综合 ROI 2.3x / 首年净 ROI 57.5% / 次年净 ROI 117.8%"徽标。可以向董事会汇报了。

## 二、新发现问题

| # | 级别 | 页面 | 期望 vs 实际 |
|---|------|------|--------------|
| 1 | **严重** | 协作空间/任务中心 | 期望：新增"不能审核自己发起的任务"约束后，系统指派的审核人应是别人。实际：@派活时 `_pick_reviewer` 仍把审核人指派为发起人本人（任务#7 系统消息"待人工审核（审核人：师圆圆）"、reviewer_id=3），但我点击审核被 403"不能审核自己发起的任务"。**被指派的审核人永远无权审核**，任务只能靠其他同事在全量看板里主动"捞"（他人的"只看待我审核"过滤里看不到此任务）。本次复测②是靠李乐平（id=2）代为驳回/通过才走完流程。修复老问题7时引入，建议派工时把审核人默认指派为"非发起人的教练/骨干"。 |
| 2 | 一般 | 协作空间·私聊打磨区 | 期望：打磨稿与其他区一样渲染排版。实际：私聊区 AI 回复以原始 Markdown 文本展示（`##`、`###`、`**` 原样可见），Agent 区交付卡片却能正常渲染，两区体验不一致（证据：`shiyuanyuan_ws7_private.png`）。 |
| 3 | 一般 | 协作空间·私聊打磨区 | 期望：建议派活对象与需求、当前工作区匹配。实际：我在"经营数据日报"工作区提经营周报需求，智能体却建议"@外贸跟单数字员工"（既非本区成员、也非董事办员工），照做会把活派给错误的员工。 |
| 4 | 建议 | 协作空间·Agent 执行区 | 已被驳回取代的 v1 交付卡片仍显示"通过/驳回"按钮（点击会 400），建议旧版卡片置灰或隐藏按钮。 |

## 三、顺带观察（第一轮其他问题）

- 问题5（派活不知@谁）：已通过场景绑定员工 + 立项说明标注责任员工解决。✅
- 问题7（自审自批）：已加 403 约束，但实现方式产生了新问题1。⚠️
- 问题8（动态流位置）：驾驶舱首屏 KPI 卡已增加口径说明小字（如"口径：试点中+已验收/场景总数"），可读性提升。
- 任务中心新增"只看待我审核"开关、"+新建任务"按钮，看板五列依旧清晰（`shiyuanyuan_tasks.png`）。

## 四、学习门槛重新评分

**第一轮 4/5 → 本轮 4.5/5。** 加分：场景库直接显示绑定员工、立项说明写明责任员工、投入构成与 ROI 一目了然、交付卡片徽标语义自明（v2/按驳回意见修订/待审核）、私聊区有"不会派活"的提示文案。仍扣 0.5：被指派的审核人却不能审这一矛盾会让第一次碰到的用户愣住（"审核人是我，为什么 403？"），私聊区 Markdown 原文也显得粗糙。

## 五、总体判定

**修复后可落地**。第一轮三个必须修的问题（立项不绑员工/重做空转/投入口径）全部真修复且有界面证据，主链路"立项→派活→驳回重做→通过→私聊打磨"完整可走通；但新问题1（审核人指派与禁止自审冲突，严重）需修掉——否则每个任务的审核都要靠同事"偶遇式"接力，扩围到 65 个员工时会堵。
